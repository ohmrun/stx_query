
using stx.core.Lift;
using stx.query.Lift;

import stx.assert.Package;

import stx.query.head.Data;
import stx.query.Package;
import stx.query.Methods.*;

class Main {
	static function main() {
		var test = new Test();
				test.run();
	}
}